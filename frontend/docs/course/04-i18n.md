# Vidéo 04 - Internationalisation (i18n)

## 🎬 Durée: 10 minutes

**Contenu identique à la vidéo 03 précédente avec ajouts:**

### Nouveautés à Mentionner:

**Intég ration avec AuthProvider (Provider Order):**
```tsx
// app/providers.tsx
<AuthProvider>      {/* 1er - Les autres providers en dépendent */}
  <I18nProvider>    {/* 2ème - Peut utiliser user.language */}
    <ThemeProvider> {/* 3ème - Peut utiliser language pour labels */}
      {children}
    </ThemeProvider>
  </I18nProvider>
</AuthProvider>
```

**Sauvegarder la langue dans le profil utilisateur:**
```tsx
// Quand l'utilisateur change de langue
const handleLanguageChange = (newLang: Language) => {
  setLanguage(newLang);
  // Optionnel: sauvegarder dans le profil utilisateur
  updateUser({ preferredLanguage: newLang });
};
```

[Contenu standard i18n: fichiers JSON, provider, hook useTranslation, sélecteur de langue]

---

## ➡️ Vidéo Suivante

[Vidéo 05: Dashboard Avancé avec Recharts](./05-dashboard-avance.md)
